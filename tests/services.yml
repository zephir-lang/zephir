services:
  Psr\Log\LoggerInterface:
    public: true

  Zephir\CompilerFile:
    public: true
    arguments:
      $config: '@Zephir\Config'
      $aliasManager: '@Zephir\AliasManager'
      $filesystem: '@Zephir\FileSystem\FileSystemInterface'
